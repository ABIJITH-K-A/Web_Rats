AUTOMATION:

AUTO_ASSIGN:
  - based_on:
      - skill_match
      - availability
      - rating
  - fallback → admin_assign

AUTO_REMINDER:
  - worker_delay → notify + escalate
  - payment_pending → remind_client

AUTO_CANCEL:
  - inactivity > threshold → cancel

AUTO_ESCALATION:
  - dispute unresolved → escalate higher role

AUTO_WALLET:
  - pending → withdrawable (after 2–3 days)